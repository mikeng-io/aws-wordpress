# Docs

| Doc | Purpose | Status |
|---|---|---|
| `study-design.md` | Measurement protocol: instruments, controls, repetitions, provenance, experiment queue | **not yet written** |
| `findings/` | One file per resolved hypothesis, citing the results that resolved it | empty until E0-E3 land |
| `superpowers/specs/` | Per-wave apparatus specs | empty |

The hypothesis register itself lives in [`../hypotheses/`](../hypotheses/), not here,
because it is the spine of the repo rather than documentation about it.
